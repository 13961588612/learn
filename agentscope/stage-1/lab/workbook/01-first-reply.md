# 实验 1：首次 Agent.reply

## 步骤

1. 配置 `.env` 中 `OPENAI_API_KEY` 或 `DASHSCOPE_API_KEY`
2. `uv run python agentscope/stage-1/lab/scripts/01_first_reply.py`
3. 记录返回 Msg 的 `role`、`get_text_content()`

## 观察题（写入 experiments/01-notes.md）

1. 使用的 model 名称？
2. reply 耗时大约多少秒？
3. 若无 Key，脚本如何提示？

## 验收

- [ ] 成功或明确跳过原因
